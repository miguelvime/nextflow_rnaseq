/*
 * nextflow.config
 * Configuración del pipeline RNA-seq GSE52778
 */

params {
    samplesheet    = "${projectDir}/assets/samplesheet.csv"
    genome_fasta   = "${projectDir}/data/genome/GRCh38.fa"
    genome_gtf     = "${projectDir}/data/genome/GRCh38.gtf"
    star_index_dir = "${projectDir}/data/star_index"
    outdir         = "${projectDir}/results"
}

profiles {
    docker {
        docker.enabled    = true
        docker.runOptions = '-u $(id -u):$(id -g)'
    }
}

process {
    cpus   = 2
    memory = '4.GB'
    time   = '4.h'

    withName: 'STAR_INDEX' { cpus = 8; memory = '32.GB' }
    withName: 'STAR_ALIGN' { cpus = 8; memory = '32.GB' }
}
